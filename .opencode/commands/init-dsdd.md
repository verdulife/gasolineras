---
description: Initialize additive DSDD project metadata
---

Run `npx --no-install dsdd init-dsdd $ARGUMENTS` in the current project. Report the JSON result and preserve
all existing files outside the DSDD-owned block. Do not implement a second initialization
logic in the agent.
