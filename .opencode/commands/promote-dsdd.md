---
description: Propose or apply a governed DSDD promotion
---

Run `npx --no-install dsdd promote-dsdd $ARGUMENTS` in the current project. Preserve the CLI's explicit approval,
adjacent-scope, identity, provenance, conflict and dry-run rules. Do not promote implicitly.
