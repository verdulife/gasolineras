---
description: Represent the canonical Project Design System visually
---

Run `npx --no-install dsdd generate-visual-dsdd $ARGUMENTS` in the current project. Treat the result as a
framework-neutral representation only. Never invent tokens, components, decisions or
canonicality, and preserve unresolved conflicts and provenance.
