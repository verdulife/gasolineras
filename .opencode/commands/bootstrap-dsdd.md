---
description: Bootstrap the project Design System through explicit approval
---

Run `npx --no-install dsdd bootstrap-dsdd $ARGUMENTS` in the current project. Keep the CLI's explicit approval,
dry-run, conflict and rollback semantics. Do not infer approval or invent Design System data.
